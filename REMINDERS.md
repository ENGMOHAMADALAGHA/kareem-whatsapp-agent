# 📌 تذكيرات معلقة (بطلب الشريك)

## 1. أوامر الطاقم واتساب: `قف` / `شغّل` ✅
- **الفكرة:** صاحب العيادة يبعت `قف` من رقمه المسجل (staffPhone) رداً على أي محادثة
  → البوت يسكت تلقائياً (takeover) على تلك المحادثة.
- `شغّل` → يرجّع البوت لنفس المحادثة.
- **الحالة:** مُنفَّذ في webhook.mjs (قبل فحص takeover عمداً حتى يعمل `شغّل` مع مُسكَت)
  — رقم المُوظف من `features.staffPhone` أو `STAFF_PHONE` العام، والرسائل تسجَّل وتُرسل للموظف نفسه.
- للتحقق: من رقم الموظف أرسل `قف` → يتوقف البوت، ثم `شغّل` → يعود.

## 2. (أُنجز) تنبيهات المالك واتساب ✅
- حجز جديد / دفع مؤكد / تصعيد / إيصال مشبوه → `notifyOwner` (كوميت c7d73d4).
