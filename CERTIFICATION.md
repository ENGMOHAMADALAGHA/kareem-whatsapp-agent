# ✅ شهادة جاهزية منصة وصل (White-Face Protocol)

> **القاعدة الجديدة:** لا يُقال "جاهز" لأي ميزة إلا بعد مرورها بهذا البروتوكول كاملاً —
> فحص كل endpoint حي + كل زر/ID بالواجهة + دورة مستخدم حقيقية. أي "نسيت" مستقبلية تعني
> أن البروتوكول لم يُطبّق، لا أن الكود رُوجِع.

## آخر فحص شامل — 2026-09-08 — **38/38 ناجح**

### البوابات والعلامة
| الفحص | النتيجة |
|---|---|
| `GET /` يحمل اسم Wasl | ✅ PASS |
| `/admin/` بدون دخول → 401، بدخول → كونسول كامل | ✅ PASS |
| `/portal/` عامة 200 | ✅ PASS |
| 404 بصيغة JSON موحدة | ✅ PASS |

### دورة البوت الكاملة (Tenant Lifecycle)
| الفحص | النتيجة |
|---|---|
| إنشاء (201 + خطة trial) / قراءة (توكن مخفي) / تعديل (خطة + staffPhone) / فحص ربط / حذف | ✅ PASS |
| فحص الربط الحي على رقم تجريبي | ✅ linked=true |

### الدعوات والمستخدمون
| الفحص | النتيجة |
|---|---|
| دعوة + رابط بوابة (إرسال مغلق) | ✅ PASS |
| إنشاء مكرر → 409 (منع الاستيلاء) | ✅ PASS |
| قائمة المستخدمين | ✅ PASS |

### القراءات (11 endpoint)
orders/summary, orders, appointments, broadcasts, report, csat, crm, inbox,
محادثة مفردة, waiting, queue, export.csv, inbox.html — ✅ PASS جميعاً.

### مسارات التحقق من الأخطاء
confirm/cancel لوهمي → 404، بث/إرسال ناقص → 400، takeover ناقص → 400،
تذكير جاف → 200 بدون إرسال، دخول خاطئ → 401، تحقق webhook خاطئ/بدون توقيع → 403 — ✅ PASS.

### الواجهة (73 معرّفاً)
كل `$("id")` موجود (ثابت أو مُنشأ ديناميكياً: toastBox, loginOv, liTenant...) — ✅ PASS.

### ما لم يُفحص حياً (يحتاج واتساب حقيقي)
إرسال رسائل فعلية، استقبال webhook حقيقي، تحقق إيصال AI، تنبيهات الطاقم —
تُفحص عبر خطة تست الساعة 2 ليلاً (رقم تجريبي + staffPhone) قبل أي عرض على عميل.
