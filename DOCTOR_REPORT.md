# التقرير التفصيلي لمشروع وكيل المبيعات الذكي على واتساب (كريم)
## منصة SaaS متعددة البوتات — تقرير الإنجاز الكامل

---

## 1. فكرة المشروع وأهدافه
منصة SaaS تتيح لأي نشاط تجاري (متجر، عيادة، مزرعة) وكيل مبيعات ذكياً على واتساب: يرد تلقائياً 24/7، يعرض المنتجات، يعالج الاعتراضات السعرية، يستقبل الطلبات والحجوزات، ويحوّل الحالات الحساسة لموظف بشري — كل بوت بهويته وأسعاره وقدراته الخاصة، وجميعها تُدار من سيرفر ولوحة تحكم واحدة.

**الأهداف المحققة:**
- أتمتة الرد على الاستفسارات المتكررة وتحويلها لطلبات وحجوزات
- عزل تام بين بيانات العملاء (كل بوت لا يرى بيانات غيره — مثبت باختبارات)
- توثيق كل تفاعل: النية، التصعيد، الطلبات، التقييمات
- قابلية إضافة نشاط جديد بدون سيرفر إضافي

---

## 2. البنية التقنية

| الطبقة | التقنية |
|---|---|
| اللغة والتشغيل | Node.js (ES Modules) + Express |
| الذكاء الاصطناعي | Google Gemini (flash-lite) بمخرجات JSON منظمة + محاكاة احتياطية |
| قاعدة البيانات | PostgreSQL على Supabase عبر Prisma ORM (7 جداول) |
| التكامل | WhatsApp Cloud API الرسمي من Meta |
| النشر | GitHub + Render (نشر تلقائي) |
| المصادقة | Basic Auth للسوبر أدمن + JWT لحسابات العملاء + bcrypt |

**هيكل الكود (طبقات منفصلة):**
```
src/config      الإعدادات
src/security    العزل (tenantDb) + التوقيع + حد المعدل
src/whatsapp    الإرسال (نص/أزرار/صور)
src/memory      الذاكرة + منع التكرار
src/ai          الشخصية + نموذج الذكاء
src/inbox       Takeover + Inbox
src/jobs        الطابور + المجدولات
src/web         الراوتات (webhook/admin/portal/billing)
```

---

## 3. المميزات المنفذة والمختبرة

| الميزة | الوصف | الحالة |
|---|---|---|
| عربي/إنجليزي + فويس | لهجة أردنية، تفريغ صوتي عبر Gemini | مختبر برقم حقيقي ✅ |
| أزرار وصور تفاعلية | أزرار Meta + صور منتجات | ✅ |
| الطلبات والدفع | إنشاء تلقائي، منع التكرار، دفع محافظ (تحويل + لقطة + تأكيد بشري)، Stripe جاهز | ✅ |
| الحجوزات | حجز + منع تعارض + بدائل + انتظار تلقائي + تذكير | ✅ |
| فرز طبي أولي | 3 أسئلة + تصنيف طارئ/عادي (مركونة بانتظار رأي عيادات) | تجريبي |
| سلة مهجورة | تذكير مجمع برسالة واحدة | ✅ |
| CRM | سجل أحداث + Webhook خارجي + تصدير CSV | ✅ |
| بث جماعي | حتى 50 رقم + سجل | ✅ |
| تقييم CSAT | 1-5 بعد الشراء + إحصائيات | ✅ |
| استعلام الطلب | "وين طلبي ord_xxx؟" من قاعدة البيانات | ✅ |
| تقرير التوفير | رسائل/إيرادات/تقييم/ساعات موفرة | ✅ |
| بوابة العميل | حساب + JWT + نسيت كلمة السر عبر واتساب + Kill switch | ✅ |

---

## 4. الأمن والجودة (رداً على ملاحظات التحكيم)

| الملاحظة | المعالجة |
|---|---|
| JSON لا تصلح للإنتاج | ترحيل كامل إلى PostgreSQL + Prisma، وحُذفت الملفات نهائياً |
| التصعيد بدون Realtime DB | Takeover دائم + Inbox حي (تحديث كل 4 ثواني) |
| تأكيد الدفع الذاتي | Stripe Webhook موقع + تحقق من الجلسة + تأكيد المحفظة بشري |
| حقن Webhook | تحقق HMAC من توقيع Meta (مفعل إنتاجياً) |
| الرد المكرر | رد 200 فوري + بصمة wamid + منع طلب مكرر + تجميع التذكير |
| تسرب بين البوتات | tenantDb: النطاق إجباري + اختبار اختراق 6/6 |
| تخمين الدخول | حد 5 محاولات/دقيقة + JWT + bcrypt |

---

## 5. الاختبارات
- 8 سيناريوهات آلية (استفسار/شراء/اعتراض/تصعيد) — ناجحة
- اختبارات تكامل حية: فويس حقيقي، طلب مكرر، تعارض حجوزات، طوارئ، انتظار، توقيع، طابور
- تجربة ميدانية على واتساب برقم تجريبي معتمد من Meta

---

## 6. المتبقي (خارطة الطريق)
1. Redis + BullMQ (عند الحمل العالي)
2. WebSockets للـ Inbox اللحظي
3. بوابات دفع محلية (تحتاج سجل تجاري)
4. رقم واتساب حقيقي + Render Pro + اختبار حمل
5. قرار الفرز الطبي بعد مقابلات العيادات

---
*2026 — منصة multi-tenant إنتاجية: بوتان حيان (رياضة + أسنان) على نفس السيرفر*
