name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
- name: Ping Render
        run: |
          curl -s https://kareem-whatsapp-agent.onrender.com/healthz | head -c 200
          curl -s https://kareem-whatsapp-agent.onrender.com/ | head -c 200
          echo "Ping done"
